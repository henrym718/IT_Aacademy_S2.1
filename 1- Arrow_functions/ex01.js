/**
 * Conversión de función: Tiene una función add que acepta dos parámetros y devuelve su suma.
 * Conviértelo en una función de flecha. Por ejemplo: function add(a, b) {return a + b;}.
 */

const add = (a, b) => a + b;
console.log(add(10, 20));
