/**
 * Función de flecha sin parámetros: Cree una función de flecha llamada randomNumber
 * que nonecesite parámetros y devuelva un número aleatorio entre 0 y 100.
 */

const randomNumber = () => Math.floor(Math.random() * 101);
console.log(randomNumber());
