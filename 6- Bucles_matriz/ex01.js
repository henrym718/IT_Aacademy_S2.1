/**
 * forEach: Tienes una matriz de nombres. Use forEach para imprimir cada nombre en la
 * consola: deja que los nombres = ['Anna', 'Bernat', 'Clara'];
 */

const nombres = ["Anna", "Bernat", "Clara"];

nombres.forEach((name) => console.log(name));
