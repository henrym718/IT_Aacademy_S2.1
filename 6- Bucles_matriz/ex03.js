/**
 * filtro: Tienes una matriz de números. Utilice el filtro para crear una nueva
 * matriz que solo contenga números pares. deja que numeros = [1, 2, 3, 4, 5, 6];
 */

const numeros_pares = [1, 2, 3, 4, 5, 6].filter((num) => num % 2 === 0);

console.log(numeros_pares);
