/**
 * Buscar: Usted tiene una matriz de números [1, 10, 8, 11].
 * Utilice la función de búsqueda para encontrar el primer número
 * que sea mayor que 10.
 */

const numbers = [1, 10, 8, 11];

const result = numbers.find((num) => num > 10);

console.log(result);
